package com.ylink.fullgoal.delete;

import com.leo.core.util.JavaTypeUtil;
import com.leo.core.util.TextUtils;

public class DHelper {

    //MainViewControllerApi测试

    /*private void test(){
        String serialNo = "20180606120013246000070";
        String imageId = "100022";
        uApi().queryUserData();
        uApi().queryDepartmentData();
        uApi().queryProjectData(getDepartmentCode());
        uApi().queryProcessData();
        uApi().queryCostIndexData();
        uApi().queryResearchReportData();
        uApi().imageUpload("1", serialNo, true, new File(
                "/storage/emulated/0/Android/data/com.ylink.fullgoal/files/photo/1528272108082217.jpg"));
        uApi().imageDelete(serialNo, imageId);
    }*/

    //ApiBean 输入监听

    /*if (isMoneyInputType() && !TextUtils.isEmpty(getTextViewFormat()) && !clear) {
        String ss = String.format(getTextViewFormat(), JavaTypeUtil.getfloat(str, 0));
        if(!TextUtils.equals(ss, tag)){
            clear = true;
            text.delete(0, text.length());
            tag = ss;
            text.append(ss);
            clear = false;
        }
    }*/

}
